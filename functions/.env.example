# Firebase Functions Environment Variables
# Copy this file to .env.local and fill in your values for local development
# NEVER commit .env.local to version control

# OpenAI-compatible API key
# Required for AI slide generation
AI_API_KEY=your-api-key-here

# AI API base URL
# Default: https://api.openai.com/v1
# Can be overridden for other providers (Azure OpenAI, local models, etc.)
AI_BASE_URL=https://api.openai.com/v1

# AI model name
# Default: gpt-4o-mini
# Must support JSON mode / structured outputs
AI_MODEL=gpt-4o-mini

# Optional: Enable debug logging
DEBUG=false

