{
  "name": "functions",
  "type": "module",
  "scripts": {
    "build": "npm --prefix ../shared/slideSpec run build && npm --prefix ../shared/errors run build && npm --prefix ../shared/utils run build && npm --prefix ../shared/validation run build && tsc && node scripts/fix-esm-imports.js && node scripts/copy-shared-package.js",
    "serve": "npm run build && firebase emulators:start --only functions",
    "deploy": "npm run build && firebase deploy --only functions",
    "lint": "eslint --ext .ts src",
    "test": "vitest",
    "test:ui": "vitest --ui",
    "test:coverage": "vitest --coverage"
  },
  "engines": {
    "node": "20"
  },
  "main": "lib/index.js",
  "dependencies": {
    "@plzfixthx/slide-spec": "file:../shared/slideSpec",
    "@plzfixthx/errors": "file:../shared/errors",
    "@plzfixthx/utils": "file:../shared/utils",
    "@plzfixthx/validation": "file:../shared/validation",
    "@types/sharp": "^0.31.1",
    "firebase-admin": "^12.6.0",
    "firebase-functions": "^6.0.1",
    "pptxgenjs": "^4.0.1",
    "sharp": "^0.34.4",
    "undici": "^7.16.0",
    "zod": "^4.1.12",
    "zod-to-json-schema": "^3.24.6"
  },
  "devDependencies": {
    "@typescript-eslint/eslint-plugin": "^5.12.0",
    "@typescript-eslint/parser": "^5.12.0",
    "eslint": "^8.9.0",
    "eslint-config-google": "^0.14.0",
    "eslint-plugin-import": "^2.25.4",
    "firebase-functions-test": "^3.1.0",
    "typescript": "^5.7.3",
    "vitest": "^1.0.0"
  },
  "private": true
}
